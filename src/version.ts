export const VERSION = '0.1.0';

/** Version of the policy schema understood by this build. */
export const POLICY_SCHEMA_VERSION = '1';

/** Version of the audit record schema understood by this build. */
export const AUDIT_SCHEMA_VERSION = '1';
