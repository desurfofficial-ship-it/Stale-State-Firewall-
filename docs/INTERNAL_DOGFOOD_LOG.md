# Internal Dogfood Friction Log

Evidence-first friction record for continuous internal dogfooding
(continuous-dogfood milestone §21). Every entry comes from an actual
dogfood/adoption/integration session — nothing speculative. Severity:
P0 security / P1 correctness / P2 operational / P3 developer experience.
P3s are collected here and prioritized by frequency and impact; they are
NOT auto-fixed, so the evidence can accumulate first.

| ID | Date | Workflow | Operation | What happened | Friction | Root cause | Severity | Workaround | Recommended fix | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| FL-1 | 2026-09-06 | Incident drill (§25) | `inspectState` on a fresh in-memory provider | `no state provider is configured for source "memory"; configure one in ssf.config.yaml under providers` even though the provider WAS registered | The operator is sent to fix the config when the actual cause is that the resource was never seeded — the in-memory provider only `supports()` resources that exist | `InMemoryStateProvider.supports()` checks `resources.has(...)`; the error message in `inspectState()` is generic and blames configuration | P3 | Seed with `provider.put(...)` before inspecting (canonical example does this) | Distinguish "no provider for source" from "provider present but resource unknown" in the error text | open |
| FL-2 | 2026-09-06 | Incident drill (§25) | seeding a memory resource | `provider.mutate(...)` refuses with `cannot mutate unknown resource` — `put()` must be called first | Two-step seeding is not obvious from the API names | `mutate` intentionally only updates existing resources (it bumps versions); creation is `put` | P3 | Follow the canonical example (`put` → `mutate`) | Document the put/mutate split on the provider surface or fold creation into `mutate` behind an explicit flag | open |
| FL-3 | 2026-09-06 | CI integration (§4) | setting up the dogfood CI gate | The repository had NO `.github/workflows/` at all — the "continuous" loop had no enforcement point before this milestone | Every gate previously depended on discipline (local runs); nothing prevented a regressing push | CI had never been created for this internal-only tool | P2 (operational) | — (fixed this milestone) | CLOSED: `ci.yml` (offline gates + offline dogfood on push/PR) + `dogfood-live.yml` (manual, protected, sandbox-only) | closed 2026-09-06 |
| FL-4 | 2026-09-06 | Adoption workflow (§13, scenario 13 step G) | CAS-window race loser on live GitHub | The refused condition-failure result carries `observed_version: null` — GitHub's 409 response does not include the current blob sha | The loser's recovery says "re-observe fresh state" but cannot say what the winner's version IS; an extra GET is needed | GitHub Contents API 409 body does not carry the current sha | P3 | Re-observe (which the recovery contract mandates anyway) | If a future GitHub API revision exposes the current sha on conflict, map it into `observed_version` | open |
| FL-7 | 2026-09-06 | Live workflow on Actions runners (§6, scenario 13 step A) | server-truth readback immediately after a satisfied CAS mutation | Actions run `34020575403`: GitHub confirmed the write (`conditional=satisfied`) but the immediate bare GET returned the PRE-write content (`replicas: 1`) — a harness false failure; the firewall and the GitHub CAS behaved correctly | GitHub's Contents API serves short-TTL caches keyed by URL: a bare GET right after a write can read a stale copy. Local network paths never hit it; Actions runners did | harness verification-layer readback, not an enforcement defect (the provider's own fresh reads use `If-None-Match` conditional requests and were fresh throughout the same run) | P2 (live-gate reliability) | — (fixed same day) | CLOSED 2026-09-06: every harness GET carries a unique query param (cache miss = fresh read); the four server-truth readbacks (A/D/E/G) use a bounded eventual-consistency window that is used ONLY after the provider has confirmed the mutation outcome — never to retry or decide one. Verified by Actions run `34020936423` (success, 14/14) and local runs showing `reads=1` everywhere | closed 2026-09-06 |
| FL-8 | 2026-09-06 | Live workflow setup (§6) | configuring the `dogfood-live` environment secret via API | The campaign fine-grained PAT can create the environment (HTTP 200) but CANNOT write environment-scoped secrets (HTTP 403 "Resource not accessible by personal access token") | fine-grained PAT lacks the Actions-secrets permission scope; environment secrets need a broader token than repo admin | P2 (operational) | repo-scoped secret `SSF_GITHUB_TOKEN` written instead — jobs in the environment inherit repo secrets, so the isolation contract (manual trigger + environment gate + fail-closed guard) is unchanged | If environment-scoped secrets are wanted, an admin with the required scope must write it | closed 2026-09-06 (repo secret operational) |
| FL-5 | 2026-09-06 | Trust-domain review (§10) | `ssf doctor` before this milestone | The storage check said only "store opened and migrated" — two deployments accidentally pointing at the same `ssf-state.db` were undetectable from doctor output | Store identity invisible; cross-environment sharing would mix audit trails silently | No resolved-path surfacing existed | P2 (operational) | — (fixed this milestone) | CLOSED: `firewall.storeDescription` + doctor now prints the resolved absolute store path and the one-store-per-trust-domain rule | closed 2026-09-06 |
| FL-6 | 2026-09-06 | HTTP checklist (§8) | redirect behavior verification | Checklist item 5 (redirects) had no local verification — the sandbox rig covered ETag/If-Match/412 but not 3xx | An operator could not see the transport-level precondition preservation demonstrated anywhere | The sandbox server had no redirect route | P3 | — (fixed this milestone) | CLOSED: `/redirect/` route added; scenario 07 Case C verifies stale→412/no-mutation and matching→applied through a real 307 | closed 2026-09-06 |
| FL-9 | 2026-09-06 | Incident exercise (sustained-dogfood §13) | simulating a concurrent actor on the memory provider | `provider.put()` on an EXISTING resource keeps its version (`version ?? (existing ? existing.version : counter)`) — the interference silently changed content without bumping the version, so the change was CAS-invisible and the provider truthfully reported "version matches"; the agent's mutation then executed on top. The doc comment claims "a monotonic v-counter is used" without the existing-resource exception | An integrator simulating interference (or seeding updates) can build a false mental model where a changed resource still satisfies its CAS — the exact trap that makes unverified providers dangerous, reproduced locally in the fixture | fixture semantics + misleading doc comment on `InMemoryStateProvider.put` (related to FL-2's put/mutate split) | P3 (fixture DX with a security-adjacent mental-model risk; enforcement core unaffected — real providers hash content) | use `mutate()` (version-bumping) for external actors, or pass an explicit version to `put()`; the correct pattern is in the harness fixtures and canonical example | Align the `put()` doc comment with the existing-resource behavior; consider a debug-level warning when a replace does not change the version. NOT auto-fixed — evidence first | open |
| FL-10 | 2026-09-06 | Credential rotation (sustained-dogfood §5) | rotating the campaign npm token via API | The automation token can LIST tokens (`GET /-/npm/v1/tokens` → 200, metadata only) but revocation returns **403** — npm's token model requires a web session or a user token to manage tokens; an automation token cannot revoke itself | The §5 rotation chain (old → revoked → new least-privilege → config updated → verified) cannot be completed from an API-only session; the credential stays active until the owner acts in the npm UI | npm registry authorization model (automation tokens are not token-managers) | P2 (operational — rotation is an explicit owner action, now with exact evidence) | none — see the rotation runbook in docs/SUSTAINED_INTERNAL_DOGFOOD_REPORT.md | Owner revokes the campaign npm token at npmjs.com → Access Tokens; also least-privilege-review the sibling tokens on the same account | open (owner action; re-verified 2026-09-06: DELETE self → 403 with same-day evidence) |
| FL-11 | 2026-09-06 | Live dogfood harness (operational-closure §8) | running `npm run dogfood:github` without SSF_GITHUB_TOKEN | The harness loudly labeled the two live scenarios `SKIPPED` with the reason and printed `SKIPPED=2` in the summary — but exited **0** with `HARNESS RESULT: PASS`. A scripted caller reading only the exit code would conclude live CAS coverage ran | Exit-code-level success for a run that explicitly requested (and did not get) live coverage; the Actions workflow guard already failed closed, so the gap was local-only | Two layers of the same fail-closed contract existed (workflow guard) but not the harness itself | P3 (observability/DX, fail-closed defense-in-depth) | none — fixed same session | CLOSED 2026-09-06: `run-all.mjs` now treats `--with-github` without the credential as an ENVIRONMENT FAILURE (exit 1) with the same operator guidance the workflow prints; verified both polarities locally (exit 1 without token, 14/14 PASS with token) | closed 2026-09-06 |

False-positive / false-negative tracking (§25/§26 of the operationalization
milestone, continued): this milestone recorded **0 false positives** (every
block was a correct stale/CAS/dependency rejection) and **0 false negatives**
(no action that should have been blocked ever executed — verified live on
GitHub by scenarios 12 and 13, and against SQLite persistence by scenario 14's
three crash boundaries). Two scenario-authoring errors during scenario 13
development were caught by review before first run and fixed; no firewall
defect was found. FL-7 is explicitly NOT a false negative: the provider had
applied the mutation (as `conditional=satisfied` truthfully reported); only
the harness's verification-layer readback saw a stale cache copy.

Sustained-dogfood milestone (2026-09-06) tracking: **0 false positives, 0
false negatives** across the baseline gates, the live 14/14 run, and the new
WF-2 dependency-update workflow (8 steps: 6 expected outcomes honored, 1
documented DF-F2 boundary, 1 auditability check). FL-9 is explicitly NOT a
false negative: the memory fixture's `put()` did not bump the version, so the
provider's own bookkeeping said the version matched — the firewall enforced
exactly the CAS contract the provider reported. The trap is recorded and the
fix recommendation is documentation-level.

## Dogfood run history (sustained-dogfood milestone §8)

Durable evidence log of meaningful dogfood runs. Scheduled runs from
`.github/workflows/scheduled-dogfood.yml` append here over time. No sensitive
payloads are ever recorded — refs, verdicts, and counters only.

| Date | Commit | Workflow | Provider | Scenarios | Result | Security failures | Condition failures | Unknown outcomes | Recovery events | Operator intervention | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-09-06 | 0956ec5 | offline harness (`npm run dogfood`) | memory + sandbox HTTP | 12/12 PASS (18 blocks / 21 successes / 2 boundaries) | PASS | 0 | exercised as expected blocks | 0 | n/a (offline) | none | baseline re-verification at session start; all six gates green |
| 2026-09-06 | 0956ec5 | live harness (`npm run dogfood:github`) | GitHub sandbox repo | 14/14 PASS (23 / 31 / 3) | PASS | 0 | 1 CAS-window refusal (step E) + recovery | 0 | 2 autonomous (step E loser + step D stale recovery) | none | credential-active polarity re-proven with the current PAT; FL-7 fix holding (`reads=1` everywhere) |
| 2026-09-06 | 0956ec5 (pre-push tree) | WF-2 dependency-update (`npm run dogfood:deps`, first run) | GitHub sandbox repo | 8 steps: 6 expected + 1 boundary + 1 audit check | PASS (first try) | 0 | 0 (DF-F2 boundary exercised, not a failure) | 0 | 1 autonomous (dependabot-drift DENY → recompute → new authz) | none | new workflow class; audit reconstruction + credential-hygiene checks green; metrics allowed=5 denied=1 |
| 2026-09-06 | 0956ec5 (pre-push tree) | incident exercise (`dogfood/ops/incident-exercise.mjs`) | memory + SQLite store | 4 events (legit, CAS-window refusal, replay probe, recovery) | evidence written | 0 | 1 (the incident) | 0 | 1 autonomous | operator phase ran CLI-only | §13 exercise; exposed the §19 request-content gap (fixed: `getAction` + `ssf action inspect`) and FL-9 |
| 2026-09-06 | 0956ec5 (pre-push tree) | scheduled cadence created (`.github/workflows/scheduled-dogfood.yml`) | — (infrastructure) | daily offline cron + weekly protected live call | pending first Actions verification | — | — | — | — | — | ordinary CI remains credential-free; live leg reuses the fail-closed guard via `workflow_call` |
| 2026-09-06 | dc81e28 | sustained cadence dispatch (`run_live=true`) — Actions run 34025069489 | GitHub sandbox repo (live leg) | both legs: offline dogfood PASS + live leg PASS via `workflow_call` with credential guard | PASS | 0 | 0 | 0 | n/a | manual dispatch (schedule not yet due) | first full cadence-chain verification: offline leg credential-free, live leg protected, guard ran, live harness 14/14 |
| 2026-09-06 | 012ed3f (working tree) | offline harness (`npm run dogfood`) at operational-closure baseline | memory + sandbox HTTP | 12/12 PASS (18 blocks / 21 successes / 2 boundaries) | PASS | 0 | exercised as expected blocks | 0 | n/a (offline) | none | independent baseline re-verification: 314/314 tests, build/typecheck/lint/hygiene PASS |
| 2026-09-06 | f0c6a1f (working tree) | live harness (`npm run dogfood:github`) after FL-11 fix | GitHub sandbox repo | 14/14 PASS (23 / 31 / 3) | PASS | 0 | 1 CAS-window refusal + autonomous recovery (step E) | 0 | 2 autonomous | none | FL-11 fix verified: harness now fails closed without credentials (exit 1), then ran live with the current credential — `reads=1` everywhere, hotfix preserved, `retry_safety` honored |
